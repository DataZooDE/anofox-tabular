require anofox_tabular

statement ok
LOAD anofox_tabular;

# Note: Postal tests are in anofox_postal.test
# anofox_postal_status() is only available when HAVE_LIBPOSTAL=1 (Linux builds)
# macOS CI builds skip libpostal due to long build times

query T
SELECT email_is_valid('user@example.com');
----
true
